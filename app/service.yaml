#apiVersion: v1
#kind: Service
#metadata:
#spec:
#  selector:
#    app: wil
#  ports:
#  - protocol: TCP
#    port: 8888
#    targetPort: 8888
apiVersion: v1
kind: Service
metadata:
  name: wil-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8888
      nodePort: 30081
  selector:
    app: wil
